<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OpenBracket IE - Fresh Agricultural Exports from India</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* ============================================
           GLOBAL STYLES & RESET
           ============================================ */

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: #161b66;
            --secondary-color: #ffffff;
            --accent-color: #2d3eb5;
            --text-dark: #1a1a1a;
            --text-light: #666666;
            --border-color: #e0e0e0;
            --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 10px 30px rgba(22, 27, 102, 0.15);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            background-color: #fafafa;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* ============================================
           HEADER & NAVIGATION
           ============================================ */

        .header {
            background-color: var(--primary-color);
            color: var(--secondary-color);
            padding: 20px 0;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .logo-section {
            display: flex;
            align-items: center;
            gap: 15px;
            flex: 1;
        }

        .logo-placeholder {
            font-size: 36px;
            font-weight: bold;
            color: var(--secondary-color);
            font-family: 'Georgia', serif;
            min-width: 50px;
        }

        .header-text h1 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 5px;
            color: var(--secondary-color);
        }

        .header-text .subtitle {
            font-size: 12px;
            color: #b8bce8;
            font-weight: 300;
        }

        .nav-menu {
            flex: 1;
            display: flex;
            justify-content: flex-end;
            gap: 40px;
        }

        .nav-menu ul {
            list-style: none;
            display: flex;
            gap: 40px;
        }

        .nav-menu a {
            color: var(--secondary-color);
            text-decoration: none;
            font-size: 15px;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-menu a:hover {
            color: #b8bce8;
        }

        .nav-menu a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background-color: #b8bce8;
            transition: width 0.3s ease;
        }

        .nav-menu a:hover::after {
            width: 100%;
        }

        .menu-toggle {
            display: none;
            background: none;
            border: none;
            color: var(--secondary-color);
            font-size: 24px;
            cursor: pointer;
        }

        .mobile-nav {
            display: none;
            background-color: var(--accent-color);
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .mobile-nav.active {
            display: block;
        }

        .mobile-nav ul {
            list-style: none;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .mobile-nav a {
            color: var(--secondary-color);
            text-decoration: none;
            font-size: 16px;
            font-weight: 500;
        }

        /* ============================================
           HERO SECTION
           ============================================ */

        .hero {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--accent-color) 100%);
            color: var(--secondary-color);
            padding: 100px 20px;
            text-align: center;
            min-height: 500px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .hero-content h2 {
            font-size: 48px;
            margin-bottom: 20px;
            font-weight: 700;
            line-height: 1.2;
        }

        .hero-content p {
            font-size: 18px;
            margin-bottom: 30px;
            color: #e8eaf5;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-button {
            background-color: var(--secondary-color);
            color: var(--primary-color);
            border: none;
            padding: 15px 40px;
            font-size: 16px;
            font-weight: 600;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: var(--shadow-lg);
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(22, 27, 102, 0.25);
        }

        /* ============================================
           SECTION STYLES
           ============================================ */

        .section-title {
            font-size: 36px;
            text-align: center;
            color: var(--primary-color);
            margin-bottom: 10px;
            font-weight: 700;
        }

        .section-subtitle {
            text-align: center;
            color: var(--text-light);
            font-size: 16px;
            margin-bottom: 50px;
        }

        /* ============================================
           PRODUCTS SECTION
           ============================================ */

        .products {
            padding: 80px 20px;
            background-color: var(--secondary-color);
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px;
        }

        .product-card {
            background: var(--secondary-color);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 30px;
            text-align: center;
            transition: all 0.3s ease;
            box-shadow: var(--shadow);
        }

        .product-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-lg);
            border-color: var(--accent-color);
        }

        .product-image-placeholder {
            width: 100%;
            height: 180px;
            background: linear-gradient(135deg, #e8eaf5 0%, #f0f3ff 100%);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            font-size: 48px;
            color: var(--accent-color);
        }

        .product-card h3 {
            font-size: 20px;
            color: var(--primary-color);
            margin-bottom: 15px;
            font-weight: 600;
        }

        .packing-details {
            text-align: left;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--border-color);
        }

        .packing-details p {
            font-size: 14px;
            color: var(--text-light);
            margin-bottom: 8px;
            line-height: 1.5;
        }

        .packing-details strong {
            color: var(--primary-color);
        }

        .product-btn {
            background-color: var(--primary-color);
            color: var(--secondary-color);
            border: none;
            padding: 10px 25px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .product-btn:hover {
            background-color: var(--accent-color);
            transform: scale(1.05);
        }

        /* ============================================
           COMPLIANCE SECTION
           ============================================ */

        .compliance {
            padding: 80px 20px;
            background: linear-gradient(to bottom, #f5f7ff, var(--secondary-color));
        }

        .compliance-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .compliance-card {
            background: var(--secondary-color);
            border: 2px solid var(--border-color);
            border-radius: 8px;
            padding: 40px 30px;
            text-align: center;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
        }

        .compliance-card:hover {
            border-color: var(--primary-color);
            box-shadow: var(--shadow-lg);
        }

        .compliance-icon {
            width: 60px;
            height: 60px;
            background-color: #e8eaf5;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 28px;
            color: var(--primary-color);
        }

        .compliance-card h3 {
            font-size: 20px;
            color: var(--primary-color);
            margin-bottom: 15px;
            font-weight: 600;
        }

        .address-details {
            text-align: center;
            font-size: 14px;
            line-height: 1.8;
            color: var(--text-light);
        }

        .compliance-badge {
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            color: var(--secondary-color);
            padding: 30px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .badge-text {
            font-size: 22px;
            font-weight: 700;
            margin-bottom: 5px;
        }

        .badge-subtitle {
            font-size: 14px;
            color: #e8eaf5;
        }

        .compliance-link {
            display: inline-block;
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 600;
            padding: 10px 20px;
            border: 2px solid var(--primary-color);
            border-radius: 5px;
            transition: all 0.3s ease;
            margin-top: 15px;
        }

        .compliance-link:hover {
            background-color: var(--primary-color);
            color: var(--secondary-color);
        }

        .standards-list {
            text-align: left;
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .standards-list p {
            font-size: 15px;
            color: var(--text-light);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .standards-list i {
            color: var(--primary-color);
            font-size: 16px;
        }

        /* ============================================
           INQUIRY FORM SECTION
           ============================================ */

        .inquiry {
            padding: 80px 20px;
            background-color: var(--secondary-color);
        }

        .inquiry-form {
            max-width: 700px;
            margin: 50px auto 0;
            background: var(--secondary-color);
            padding: 40px;
            border-radius: 8px;
            box-shadow: var(--shadow-lg);
            border: 1px solid var(--border-color);
        }

        .form-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 25px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-group label {
            font-size: 14px;
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 8px;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            padding: 12px 15px;
            border: 1px solid var(--border-color);
            border-radius: 5px;
            font-size: 14px;
            font-family: inherit;
            transition: all 0.3s ease;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(22, 27, 102, 0.1);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .submit-btn {
            width: 100%;
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            color: var(--secondary-color);
            border: none;
            padding: 15px;
            font-size: 16px;
            font-weight: 600;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-lg);
        }

        .form-note {
            text-align: center;
            font-size: 12px;
            color: var(--text-light);
            margin-top: 15px;
        }

        /* ============================================
           FOOTER
           ============================================ */

        .footer {
            background-color: var(--primary-color);
            color: var(--secondary-color);
            padding: 50px 20px 20px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        .footer-section h3,
        .footer-section h4 {
            margin-bottom: 15px;
            font-size: 18px;
            font-weight: 600;
        }

        .footer-section p {
            font-size: 14px;
            line-height: 1.8;
            color: #b8bce8;
        }

        .footer-section a {
            color: #b8bce8;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .footer-section a:hover {
            color: var(--secondary-color);
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 15px;
        }

        .social-links a {
            width: 40px;
            height: 40px;
            background-color: var(--accent-color);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            background-color: var(--secondary-color);
            color: var(--primary-color);
            transform: scale(1.1);
        }

        .footer-section ul {
            list-style: none;
        }

        .footer-section ul li {
            margin-bottom: 10px;
        }

        .footer-section ul a {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .hours {
            font-weight: 600;
            font-size: 15px;
        }

        .small-text {
            font-size: 12px;
            color: #8b92cc;
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid var(--accent-color);
            color: #8b92cc;
            font-size: 13px;
        }

        /* ============================================
           MODAL / SUCCESS MESSAGE
           ============================================ */

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 2000;
            align-items: center;
            justify-content: center;
        }

        .modal.show {
            display: flex;
        }

        .modal-content {
            background: var(--secondary-color);
            padding: 40px;
            border-radius: 8px;
            max-width: 400px;
            text-align: center;
            box-shadow: var(--shadow-lg);
            position: relative;
        }

        .modal-close {
            position: absolute;
            top: 15px;
            right: 15px;
            background: none;
            border: none;
            font-size: 28px;
            cursor: pointer;
            color: var(--text-light);
        }

        .modal-icon {
            font-size: 60px;
            color: #4caf50;
            margin-bottom: 20px;
        }

        .modal-content h2 {
            color: var(--primary-color);
            margin-bottom: 15px;
            font-size: 24px;
        }

        .modal-content p {
            color: var(--text-light);
            margin-bottom: 25px;
            line-height: 1.6;
        }

        .modal-btn {
            background-color: var(--primary-color);
            color: var(--secondary-color);
            border: none;
            padding: 12px 30px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .modal-btn:hover {
            background-color: var(--accent-color);
        }

        /* ============================================
           RESPONSIVE DESIGN
           ============================================ */

        @media (max-width: 768px) {
            .header-text h1 {
                font-size: 18px;
            }

            .header-text .subtitle {
                font-size: 10px;
            }

            .logo-placeholder {
                font-size: 28px;
            }

            .nav-menu {
                display: none;
            }

            .menu-toggle {
                display: block;
            }

            .hero-content h2 {
                font-size: 32px;
            }

            .hero-content p {
                font-size: 16px;
            }

            .section-title {
                font-size: 28px;
            }

            .products-grid,
            .compliance-grid {
                grid-template-columns: 1fr;
            }

            .inquiry-form {
                padding: 30px 20px;
            }

            .form-row {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .footer-content {
                grid-template-columns: 1fr;
                gap: 30px;
            }

            .hero {
                padding: 60px 20px;
                min-height: 400px;
            }

            .products,
            .inquiry,
            .compliance {
                padding: 50px 20px;
            }
        }

        @media (max-width: 480px) {
            .header-container {
                flex-wrap: wrap;
            }

            .header-text h1 {
                font-size: 16px;
            }

            .logo-placeholder {
                font-size: 24px;
            }

            .hero-content h2 {
                font-size: 24px;
            }

            .hero-content p {
                font-size: 14px;
            }

            .section-title {
                font-size: 24px;
            }

            .section-subtitle {
                font-size: 14px;
            }

            .product-card,
            .compliance-card {
                padding: 20px 15px;
            }

            .inquiry-form {
                padding: 20px 15px;
            }

            .cta-button,
            .submit-btn {
                padding: 12px 25px;
                font-size: 14px;
            }

            .hero {
                padding: 40px 15px;
                min-height: 350px;
            }

            .products,
            .inquiry,
            .compliance {
                padding: 40px 15px;
            }

            .footer-content {
                gap: 20px;
            }

            .footer-section h3,
            .footer-section h4 {
                font-size: 16px;
            }

            .modal-content {
                max-width: 90%;
                padding: 30px 20px;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <!-- HEADER SECTION -->
    <header class="header">
        <div class="header-container">
            <div class="logo-section">
                <div class="logo-placeholder">][</div>
                <div class="header-text">
                    <h1 class="company-name">OpenBracket IE</h1>
                    <p class="subtitle">Connecting Global Markets with Fresh Agricultural Excellence</p>
                </div>
            </div>
            <nav class="nav-menu">
                <ul>
                    <li><a href="#products">Products</a></li>
                    <li><a href="#compliance">Compliance</a></li>
                    <li><a href="#inquiry">Inquire Now</a></li>
                    <li><a href="#contact">Contact</a></li>
                </ul>
            </nav>
            <button class="menu-toggle" id="menuToggle">
                <i class="fas fa-bars"></i>
            </button>
        </div>
    </header>

    <!-- MOBILE NAV MENU -->
    <nav class="mobile-nav" id="mobileNav">
        <ul>
            <li><a href="#products">Products</a></li>
            <li><a href="#compliance">Compliance</a></li>
            <li><a href="#inquiry">Inquire Now</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <!-- HERO SECTION -->
    <section class="hero">
        <div class="hero-content">
            <h2>India's Premier Exporter of Fresh Produce</h2>
            <p>Delivering top-grade fresh fruits and vegetables to global markets with unwavering quality and reliability</p>
            <button class="cta-button" onclick="document.getElementById('inquiry').scrollIntoView({behavior: 'smooth'})">Get Started Today</button>
        </div>
    </section>

    <!-- PRODUCT PORTFOLIO SECTION -->
    <section class="products" id="products">
        <div class="container">
            <h2 class="section-title">Our Product Portfolio</h2>
            <p class="section-subtitle">Premium Quality Exports from India to the World</p>
            
            <div class="products-grid">
                <!-- Fresh Onions Card -->
                <div class="product-card">
                    <div class="product-image-placeholder">
                        <i class="fas fa-apple-alt"></i>
                    </div>
                    <h3>Fresh Onions</h3>
                    <div class="packing-details">
                        <p><strong>Varieties:</strong> Yellow, Red, White</p>
                        <p><strong>Packing:</strong> 25 kg bags, Bulk containers</p>
                        <p><strong>Grade:</strong> Premium A Grade</p>
                        <p><strong>Season:</strong> Year-round availability</p>
                    </div>
                    <button class="product-btn">Learn More</button>
                </div>

                <!-- Fresh Chilies Card -->
                <div class="product-card">
                    <div class="product-image-placeholder">
                        <i class="fas fa-leaf"></i>
                    </div>
                    <h3>Fresh Green & Red Chilies</h3>
                    <div class="packing-details">
                        <p><strong>Varieties:</strong> Long Green, Hot Red</p>
                        <p><strong>Packing:</strong> 10 kg boxes, Customizable</p>
                        <p><strong>Grade:</strong> Export Quality</p>
                        <p><strong>Season:</strong> Year-round supply</p>
                    </div>
                    <button class="product-btn">Learn More</button>
                </div>

                <!-- Premium Pomegranates Card -->
                <div class="product-card">
                    <div class="product-image-placeholder">
                        <i class="fas fa-seedling"></i>
                    </div>
                    <h3>Premium Pomegranates</h3>
                    <div class="packing-details">
                        <p><strong>Varieties:</strong> Sweet Seeded, Large</p>
                        <p><strong>Packing:</strong> 18-20 count/box</p>
                        <p><strong>Grade:</strong> Handpicked Premium</p>
                        <p><strong>Season:</strong> September – December</p>
                    </div>
                    <button class="product-btn">Learn More</button>
                </div>

                <!-- Seasonal Produce Card -->
                <div class="product-card">
                    <div class="product-image-placeholder">
                        <i class="fas fa-carrot"></i>
                    </div>
                    <h3>Seasonal Produce</h3>
                    <div class="packing-details">
                        <p><strong>Items:</strong> Garlic, Potatoes, Turmeric</p>
                        <p><strong>Packing:</strong> Custom bulk orders</p>
                        <p><strong>Grade:</strong> Superior Quality</p>
                        <p><strong>Availability:</strong> Season-dependent</p>
                    </div>
                    <button class="product-btn">Learn More</button>
                </div>
            </div>
        </div>
    </section>

    <!-- REGULATORY DATA & COMPLIANCE SECTION -->
    <section class="compliance" id="compliance">
        <div class="container">
            <h2 class="section-title">Regulatory Compliance & Certifications</h2>
            
            <div class="compliance-grid">
                <!-- Registered Address Card -->
                <div class="compliance-card">
                    <div class="compliance-icon">
                        <i class="fas fa-map-marker-alt"></i>
                    </div>
                    <h3>Registered Address</h3>
                    <div class="address-details">
                        <p>Shop No. 26, 1st Floor</p>
                        <p>Crystal Plaza Premises</p>
                        <p>Plot No. 18 & 27, Sector 7</p>
                        <p>Kharghar, Raigad</p>
                        <p>Maharashtra, India - 410210</p>
                    </div>
                </div>

                <!-- DGFT/IEC Compliance Badge -->
                <div class="compliance-card">
                    <div class="compliance-icon">
                        <i class="fas fa-certificate"></i>
                    </div>
                    <h3>Government Verification</h3>
                    <div class="compliance-badge">
                        <p class="badge-text">DGFT / IEC Compliant</p>
                        <p class="badge-subtitle">Verified Exporter Status</p>
                    </div>
                    <a href="https://dgft.gov.in" target="_blank" class="compliance-link">
                        Verify on Government Portal <i class="fas fa-external-link-alt"></i>
                    </a>
                </div>

                <!-- Quality Certifications -->
                <div class="compliance-card">
                    <div class="compliance-icon">
                        <i class="fas fa-check-circle"></i>
                    </div>
                    <h3>Quality Standards</h3>
                    <div class="standards-list">
                        <p><i class="fas fa-checkmark"></i> APEDA Registered</p>
                        <p><i class="fas fa-checkmark"></i> Fully Documented Exports</p>
                        <p><i class="fas fa-checkmark"></i> Cold Chain Compliant</p>
                        <p><i class="fas fa-checkmark"></i> Phytosanitary Certified</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- INTERACTIVE INQUIRY FORM SECTION -->
    <section class="inquiry" id="inquiry">
        <div class="container">
            <h2 class="section-title">Global Trade Inquiry Form</h2>
            <p class="section-subtitle">Connect with us for competitive quotes and bulk orders</p>
            
            <form class="inquiry-form" id="inquiryForm">
                <div class="form-row">
                    <div class="form-group">
                        <label for="name">Full Name *</label>
                        <input type="text" id="name" name="name" placeholder="Your Name" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email Address *</label>
                        <input type="email" id="email" name="email" placeholder="your@email.com" required>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="country">Country *</label>
                        <input type="text" id="country" name="country" placeholder="Your Country" required>
                    </div>
                    <div class="form-group">
                        <label for="product">Product Interest *</label>
                        <select id="product" name="product" required>
                            <option value="">Select a product</option>
                            <option value="Fresh Onions">Fresh Onions</option>
                            <option value="Fresh Chilies">Fresh Green & Red Chilies</option>
                            <option value="Pomegranates">Premium Pomegranates</option>
                            <option value="Seasonal Produce">Seasonal Produce</option>
                            <option value="Multiple Products">Multiple Products</option>
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="volume">Volume Required (Metric Tons) *</label>
                        <input type="number" id="volume" name="volume" placeholder="e.g., 10" step="0.1" required>
                    </div>
                    <div class="form-group">
                        <label for="phone">Contact Number</label>
                        <input type="tel" id="phone" name="phone" placeholder="Your Phone Number">
                    </div>
                </div>

                <div class="form-group full-width">
                    <label for="message">Message *</label>
                    <textarea id="message" name="message" placeholder="Tell us more about your requirements, delivery timeline, and any specific needs..." rows="5" required></textarea>
                </div>

                <button type="submit" class="submit-btn">Submit Inquiry</button>
                <p class="form-note">We typically respond within 24 hours during business days</p>
            </form>
        </div>
    </section>

    <!-- FOOTER SECTION -->
    <footer class="footer" id="contact">
        <div class="container">
            <div class="footer-content">
                <!-- Company Info -->
                <div class="footer-section">
                    <h3>OpenBracket IE</h3>
                    <p>India's trusted exporter of premium fresh fruits and vegetables to global markets.</p>
                    <div class="social-links">
                        <a href="#" title="Facebook"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" title="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
                        <a href="#" title="Twitter"><i class="fab fa-twitter"></i></a>
                        <a href="#" title="Email"><i class="fas fa-envelope"></i></a>
                    </div>
                </div>

                <!-- Quick Links -->
                <div class="footer-section">
                    <h4>Quick Links</h4>
                    <ul>
                        <li><a href="#products">Products</a></li>
                        <li><a href="#compliance">Compliance</a></li>
                        <li><a href="#inquiry">Contact Us</a></li>
                        <li><a href="#">Privacy Policy</a></li>
                    </ul>
                </div>

                <!-- Contact Information -->
                <div class="footer-section">
                    <h4>Contact Us</h4>
                    <p><strong>Primary Contact:</strong></p>
                    <p><i class="fas fa-user"></i> Rohit Patil</p>
                    <p><i class="fas fa-phone"></i> <a href="tel:+918108050990">+91 8108050990</a></p>
                    <p><i class="fas fa-envelope"></i> <a href="mailto:openbracketie@gmail.com">openbracketie@gmail.com</a></p>
                </div>

                <!-- Business Hours -->
                <div class="footer-section">
                    <h4>Global Desk Hours</h4>
                    <p><i class="fas fa-clock"></i> Monday – Saturday</p>
                    <p class="hours">09:00 AM – 06:00 PM IST</p>
                    <p class="small-text">We're here to assist with your export inquiries</p>
                </div>
            </div>

            <div class="footer-bottom">
                <p>&copy; 2024 OpenBracket IE. All Rights Reserved. | Trusted Exporter of Fresh Agricultural Products</p>
            </div>
        </div>
    </footer>

    <!-- Success Message Modal -->
    <div class="modal" id="successModal">
        <div class="modal-content">
            <button class="modal-close" onclick="closeModal()">&times;</button>
            <div class="modal-icon">
                <i class="fas fa-check-circle"></i>
            </div>
            <h2>Inquiry Submitted Successfully!</h2>
            <p>Thank you for your interest in OpenBracket IE. Our team will review your inquiry and contact you within 24 hours.</p>
            <button class="modal-btn" onclick="closeModal()">Close</button>
        </div>
    </div>

    <script>
        // ============================================
        // MOBILE MENU TOGGLE
        // ============================================

        const menuToggle = document.getElementById('menuToggle');
        const mobileNav = document.getElementById('mobileNav');

        menuToggle.addEventListener('click', () => {
            mobileNav.classList.toggle('active');
        });

        // Close mobile menu when a link is clicked
        const mobileNavLinks = mobileNav.querySelectorAll('a');
        mobileNavLinks.forEach(link => {
            link.addEventListener('click', () => {
                mobileNav.classList.remove('active');
            });
        });

        // ============================================
        // FORM SUBMISSION HANDLER
        // ============================================

        const inquiryForm = document.getElementById('inquiryForm');
        const successModal = document.getElementById('successModal');

        inquiryForm.addEventListener('submit', (e) => {
            e.preventDefault();

            // Get form data
            const formData = new FormData(inquiryForm);
            const data = {
                name: formData.get('name'),
                email: formData.get('email'),
                country: formData.get('country'),
                product: formData.get('product'),
                volume: formData.get('volume'),
                phone: formData.get('phone'),
                message: formData.get('message'),
                timestamp: new Date().toISOString()
            };

            // Log the inquiry data
            console.log('Inquiry Submitted:', data);

            // Store in localStorage for demonstration
            let inquiries = JSON.parse(localStorage.getItem('inquiries')) || [];
            inquiries.push(data);
            localStorage.setItem('inquiries', JSON.stringify(inquiries));

            // Show success modal
            showModal();

            // Reset form
            inquiryForm.reset();
        });

        // ============================================
        // MODAL FUNCTIONS
        // ============================================

        function showModal() {
            successModal.classList.add('show');
        }

        function closeModal() {
            successModal.classList.remove('show');
        }

        // Close modal when clicking outside
        successModal.addEventListener('click', (e) => {
            if (e.target === successModal) {
                closeModal();
            }
        });

        // ============================================
        // SMOOTH SCROLLING FOR NAVIGATION
        // ============================================

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                if (href !== '#') {
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                }
            });
        });

        // ============================================
        // PRODUCT CARD INTERACTIONS
        // ============================================

        const productBtns = document.querySelectorAll('.product-btn');

        productBtns.forEach(btn => {
            btn.addEventListener('click', (e) => {
                e.preventDefault();
                
                // Get product name from the card
                const productName = btn.closest('.product-card').querySelector('h3').textContent;
                
                // Scroll to inquiry form
                document.getElementById('inquiry').scrollIntoView({ behavior: 'smooth' });
                
                // Pre-fill product field in form
                const productSelect = document.getElementById('product');
                const options = productSelect.querySelectorAll('option');
                
                options.forEach(option => {
                    if (option.textContent.includes(productName)) {
                        productSelect.value = option.value;
                    }
                });
                
                // Focus on the form
                document.getElementById('name').focus();
            });
        });

        // ============================================
        // PAGE LOAD ANIMATIONS
        // ============================================

        window.addEventListener('load', () => {
            // Add animation to product cards on page load
            const productCards = document.querySelectorAll('.product-card');
            productCards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.animation = `fadeInUp 0.6s ease ${index * 0.1}s forwards`;
            });
        });

        console.log('OpenBracket IE Website Loaded Successfully');
    </script>
</body>
</html>